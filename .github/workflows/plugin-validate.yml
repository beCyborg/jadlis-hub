name: Validate Plugins

on:
  pull_request: {}
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  ci:
    uses: ./.github/workflows/plugin-ci.yml
    with:
      mode: marketplace
      claude-cli-version: "2.1.263"
    secrets: inherit
